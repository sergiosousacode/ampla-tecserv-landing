export default function Button_baixar() {
  return (
    <button
      type="button"
      disabled
      aria-disabled="true"
      title="Disponivel em breve"
      className="w-full cursor-not-allowed rounded-2xl border border-white/20 bg-white/10 px-6 py-3 text-sm font-semibold text-white/80 opacity-90 sm:w-auto sm:px-8 sm:text-base"
    >
      Baixar acesso remoto (em breve)
    </button>
  );
}
